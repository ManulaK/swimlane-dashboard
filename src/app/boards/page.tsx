import React from 'react';

export default function BoardsPage() {
	return (
		<main className="min-h-screen flex items-center justify-center" style={{ background: 'var(--color-bg)' }}>
			<h1 className="text-2xl font-bold">Boards Home</h1>
		</main>
	);
}
